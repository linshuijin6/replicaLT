# Workspace Agents

- Follow the project-specific rules in `.agent/rules.md`.
- Activate `sync-logger` for every conversation in this workspace.
- Run the `sync-logger` workflow defined in `/mnt/ssd/linshuijin/.agents/skills/sync-logger/SKILL.md` after the main task is complete.
- Treat the current workspace root as the base directory for all `sync/` files.
